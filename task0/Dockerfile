# Use the latest Ubuntu base image
FROM ubuntu:latest

# Update the repository sources list
RUN apt-get update

# Upgrade the currently installed software
RUN apt-get upgrade -y

# Set the command to run when the container starts
CMD ["echo", "Hello, World!"]
